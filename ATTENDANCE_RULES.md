# 잡핏 출퇴근·포인트 규칙 (관리자 앱 ↔ 알바생 앱 ↔ Supabase 공통 계약)

**작성 2026-08-24** · 근거: 기획안 v1.4 §4-2/§4-3/§5-5/§6 + 사장님 결정 · 코드 상수: `lib/policy.dart` (알바생 앱에 그대로 복사)

> 원칙: **출결 상태**와 **퇴근 기록**은 별개 필드. 포인트는 **종료 후 정산에서만**, **정상 출근 + 정상 퇴근**일 때만 자동.

## 1. 데이터 모델 (Supabase 권장)

| 필드 | 값 | 누가 씀 |
|---|---|---|
| `attendance.status` | `wait` 출근전 · `none` 미도착 · `ok` 출근 · `late` 지각 · `early` 조퇴 · `runaway` 무단이탈 · `absent` 결근 | 알바생 앱(GPS) → 관리자 앱(보정) |
| `attendance.checkin_at` | 출근 시각 | 알바생 앱 |
| `attendance.checkout_at` | 퇴근 시각 (null = 미처리) | 알바생 앱 / 관리자 앱 / 시스템 |
| `attendance.checkout_source` | `gps` · `approved` · `manual` · `auto` · `rejected` | 위와 같음 |
| `gps_requests` | 영역 밖 퇴근 사유 (status pending/approved/rejected) | 알바생 앱 제출 → 관리자 앱 승인 |

## 2. 출근 (알바생 앱이 기록)

1. 근무 시작 **60분 전**부터 [출근] 활성화 — **근무지 GPS 다각형 영역 안**에서만
2. 출근 흐름: 버튼 → 확인 팝업 → (계약서/안전교육 서명) → **GPS 재검증** → `status=ok`, `checkin_at`
3. 시작 시각 지나서 출근 → `late` (출근으로 인정, 경고 사유는 관리자 재량)
4. 시작 **+30분**까지 기록 없음 → 관리자 앱에 `none(미도착)`으로 표시 → 관리자가 **[수동 출근]** 또는 **[결근]** 보정

## 3. 퇴근

| 상황 | 처리 | checkout_source |
|---|---|---|
| 영역 안에서 [퇴근] | 자동 기록 | `gps` |
| 영역 밖 → 사유 입력 제출 | 관리자 앱 **[승인 · 퇴근 인정]** → 정상 퇴근 | `approved` |
| 위 사유를 **[반려]** | 시각은 남기되 **정상 퇴근 아님** | `rejected` |
| 관리자가 종료 화면에서 [퇴근 처리] | 정상 퇴근 | `manual` |
| 종료 **+6시간** 경과, 기록 없음 | 시스템 자동 퇴근 | `auto` |
| **근무 종료 전** | 알바생 앱 [퇴근] 버튼 **비활성** (`Policy.checkoutOpenAfterEnd`) — 조퇴는 관리자 표시로만. 종료 전엔 영역 밖 사유 제출만 가능 | — |
| 영역 밖 사유를 **종료 전에 제출** | 관리자 앱 카드에 '종료 N시간 N분 전 제출' 표시 → **[조퇴로 인정]**(기본) = `status=early` + 기록 `조퇴` (감사 `gps_early`, 포인트 없음) / [정상 퇴근 인정] = `approved` / [반려] | `approved` 또는 조퇴 |

- 종료 시각부터 퇴근 미처리자에게 **5분 간격 알림** (최대 6시간)
- 관리자가 **조퇴 / 무단이탈** 표시하면 퇴근 기록은 자동(`조퇴`/`이탈`) — 퇴근 미처리 목록에서 제외. 다시 출근/지각으로 되돌리면 그 기록도 지움
- **자동 퇴근 규칙 (코드 `outOf()`)**: 종료 +6h 경과 · status ∈ {ok, late} · 기록 없음 → 기록값 `'자동'` = `checkout_source=auto` (포인트 대상). "기록이 실제로 있나" 판단(GPS 사유 필터 등)은 `outRecordOf()` (자동 제외)
- 관리자가 수동 퇴근 처리하면 그 사람의 GPS 사유 제출 건은 처리 완료(`gGpsDone`)로 간주
- 관리자 앱 [퇴근 처리]도 종료 시각 이후에만 활성 (그 전엔 '근무 종료(HH:MM) 후 퇴근 처리 가능'). 조퇴·이탈·결근은 퇴근 처리 대상 아님
- **관리자 확인(더블체크)** `gVerifiedIn` / `gVerifiedOut`: 진행 중엔 출근(ok·late)자에게 [확인], 종료 후엔 퇴근 기록자에게 [퇴근 확인] — '확인됨 · 관리자명 HH:MM' 기록만 남김 (감사 `verify`, **포인트 무관**). 다시 누르면 취소, 섹션 헤더 [전원 확인]

## 4. 포인트 (종료 후 정산에서만)

```
지급 = status ∈ {ok, late}
    AND checkout_at != null
    AND checkout_source != rejected
```
- 공고 기본 **1,000P** (공고별 조정), 같이하기 보너스 +3,000P(각자), 단순변심 취소 −1,000P
- `early`(조퇴) · `runaway`(무단이탈) · `absent` · `none` → **자동 지급 없음** (PC에서 수동 판단)
- **조퇴 = 알바비·포인트 없음 (파트너사 확정 2026-08-30)** — 조퇴 기록 시 알바생 앱에 팝업 안내 (`gWorkerNotes`: '조퇴 기록 — 알바비·포인트 지급 대상 아님. 반복 시 경고 대상')
- 진행 중 화면에서는 포인트를 지급하지 않음 (승인은 "퇴근 인정"까지만)

## 5. 정정

- 종료 후 **7일**까지 앱에서 정정 가능 (이후 PC 관리자 웹)
- 모든 정정은 감사로그 기록 (누가·언제·무엇을)
- **정정은 등급 무관 즉시 반영 (2급 현장 확인용) · 감사로그** (확정 2026-08-30)
- 출결정정 포인트는 **멱등·가역** (검증 B2): 결근↔출근 반복해도 중복 지급 없음, 되돌리면 회수

## 6. 대기열

- FULL 시 대기 정원 = 모집 × **2**
- 자리 나면 1번에게 제안 → 수락 제한 **24h 전 1시간 / 24h 이내 30분** (웹과 통일 · 2026-08-30 확정)
- 초과 → 자동 거절 → 다음 대기자 / 전원 실패 → 일반 모집 재개(reopened)
- **홀드는 반려·서버 6h 만료 시 해제 → 대기 1번 자동 제안** (`afterSeatOpened()` — 공고 상세·승인 탭 공용)
- 대기열에서는 짝(같이하기) 신청 불가 — 개인만
- **시작 시각 경과 → 공고 자동 내림(알바생 앱 신청·대기 불가) · 대기열 제안 중단 · 부족 인원은 현장 직접 추가·외부인력로만** (확정 2026-08-31 · 코드 `afterSeatOpened()` 시작 후 null, 관리자 앱 '시작 후 대기열 제안 없음')

## 6-1. 신청 · 배정 (관리자 앱)

- 결정 키 `gDecided`: 신청 = `app|이름|jobId` (jobId 없으면 근무지명), 취소 검토 = `cancel|이름|근무지` — 승인 탭·공고 상세·되돌리기 모두 같은 키 (`appKey()`)
- 승인 = `approveApp()` 한 곳: `gDecided` + `gApprovedByJob[jobKey]`(확정 명단 합류) + 공지 자동 전달 + 감사로그. 되돌리기(`unapprove()`)는 확정 명단에서도 제거
- **배정 자격 검사 (`eligibilityIssue` / `checkEligibility`)** — 직접 추가·신청 승인 공용
  - 정원: 빈자리(`seatsOf`) < 필요 자리 → 불가 (같이하기는 2자리 필요)
  - 같은 날 다른 공고에 이미 배정 → 불가 (하루 1건)
  - 동일 근무지 주 N일 (CJ·롯데 4 / 컨벤션 2, 월~일) 초과 → 확인 후 강제 배정 가능 (기록)
  - 같이하기 짝 응답 대기(`buddyState=pending`) → 승인 불가
- 빈자리 = 시작 전: 정원 − 확정 / 시작 후: 정원 − 현장에 왔던 사람(출근·지각·조퇴). **퇴근자는 자리를 채운 사람** — 결근·미도착·이탈만 자리가 남
- 시작 후엔 `wait`(출근 전) = `none`(미도착)으로 취급 (`effStatus`)
- **관리자 강제 취소** (`gForceCancelled`, 시작 전 · 사유 필수 · 감사 `app_cancel_admin`): 그 사람은 모든 명단·집계·근무 내역에서 제외. 자리가 나고 대기자가 있으면 1번에게 **자동 자리 제안**
- **공고별 자동 알림 토글** (`gReminderOff`): 시작 1시간 전 확정자 푸시를 공고 단위로 끌 수 있음 (감사 `reminder`)
- 승인 대기 제한 6시간 (`Policy.approvalWaitMaxHours`) — 넘으면 빨강 표시. **서버가 6h 초과 시 홀드 해제** (앱엔 타이머 없음)
- **신청 홀드 (B안 · 전원, 확정 2026-08-30)**: 승인 대기 신청 1건 = 1자리 홀드 (협의대상 포함 · 같이하기 짝 = 2자리) — `heldOf()`. 정원 계산 `filled = 확정 + 홀드` (`liveCounts`/`seatsOf`), 다른 알바생에겐 FULL/대기열 표시. 카드 표기 `7 / 8 확정 · 홀드 1`
  - 승인 = 홀드 → 확정 전환 (자격 검사 시 본인 홀드는 빈자리로 취급 `ownHold`)
  - 반려·6h 만료·철회 = 홀드 해제 → 자리 나면 대기 1번 자동 제안
  - 직접 추가로 확정+홀드 > 정원이면 신청 카드에 '정원 초과 — 직접 추가로 자리가 찼어요' + **[대기열로 이동]** (홀드 해제 → 대기열 맨 뒤 `waiting`, 감사 `app_to_waitlist`)
- **경고 부여는 앱에서 가능 (확정 2026-08-30)** — 스위치 없음, 관리자 재량 · 3회 누적 시 협의대상 자동

## 7. ⚠ 아직 결정 안 된 것 (사장님)

1. ~~지각자 포인트~~ → **확정: 자동 지급** (문제 시 관리자 판단으로 **포인트 회수** — 메시지 필수, 알바생에게 전달)
2. ~~6시간 자동 퇴근~~ → **확정: 정상 퇴근으로 인정, 포인트 지급**
3. GPS 권한 거부 시 대안 (사진 인증 등)
4. ~~택배 조기퇴근(정상 조퇴) 처리 방식~~ → **확정: 조퇴는 알바비·포인트 없음** (파트너사, 2026-08-30)

## 8. 알바생 앱 구현 시 매칭 체크리스트

- [ ] `lib/policy.dart` 복사 — 상수·상태 문자열 동일
- [ ] 출근 버튼 활성화 = 시작 60분 전 + GPS 영역 안
- [ ] 출근 시 서명 → GPS 재검증 → `ok/late` 기록
- [ ] 퇴근: 영역 안 `gps` / 영역 밖 → 사유 화면 → `gps_requests` 제출
- [ ] 퇴근 버튼은 종료 시각 이후에만 활성화 (`Policy.checkoutOpenAfterEnd`) — 종료 전 영역 밖 사유 제출은 관리자가 조퇴/정상 퇴근 판정
- [ ] 종료 후 5분 간격 퇴근 알림
- [ ] 포인트 화면은 "정산 예정" ↔ "지급 완료" 구분 (진행 중엔 미지급)
- [ ] 조퇴 기록 시 경고 안내 팝업 표시
- [ ] 빈자리 1개일 때 짝(같이하기) 신청 차단
- [ ] 대기열에서는 짝 신청 불가 (개인만)
- [ ] 신청 즉시 1자리 홀드 → 다른 알바생에겐 FULL/대기열 표시
- [ ] (선택) 읽음 수는 알바생 앱이 알림 열 때 신호를 보내야 집계됨 (`notifications.read_at` 갱신 → 관리자 앱 알림 로그 '확인 N') — **확인(읽음) 집계 여부는 친척동생 판단 (필수 아님, 0이면 관리자 앱에 표시 안 함)**
- [ ] 계약서·안전교육 서명은 공고 토글(`jobs.contract` / `jobs.safety`)이 켜진 경우만 — 서명 완료 시 `attendance.signed_at` 기록 (관리자 앱 서명/미서명 열)
- [ ] 지오펜스: 근무지 영역 진입 시 "출근 버튼 활성화" 로컬 알림 (시작 60분 전~시작 사이에만)

## 9. 알림 로그 보존 (단체 발송만)

- 보존 항목: **본문 · 제목 · 긴급 여부 · 시각 · 발송자 · 등급 · 종류(manual 공지 / auto 자동 리마인더 / urgent_recruit 긴급 구인) · 수신자 수 · 읽음 수(선택)**
- 개별 메시지(포인트 회수·경고·조퇴 안내)는 알림 로그가 아니라 알바생별 안내(`worker_notices`)에 남김
- 자동 리마인더 = 시작 1시간 전 확정자 푸시 (`gReminderOff`로 공고별 끔) — 발송자 '시스템', 등급 없음

## 10. 취소 사유 분류 코드 (`CancelCategory` · DB·알바생 앱·관리자 앱 동일 키) — **친척동생 결정 사항 (아래 코드 표는 제안)**

| 코드 | 라벨 | 비고 |
|---|---|---|
| `normal` | 단순변심 | −1,000P 자동 차감 (경고와 별개) |
| `sick` | 질병 | |
| `family` | 가족 | |
| `transport` | 교통 | |
| `weather` | 천재지변 | 차감 면제 권고 |
| `other` | 기타 | |

- UI에는 한국어 라벨만, 저장·전송은 코드 키 (`CancelCategory.codeOf(label)` / `labelOf[code]`)
